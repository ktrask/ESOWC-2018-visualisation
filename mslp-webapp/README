#MSLP Webapp

The mslp_spread.nc and mslp_mean.nc are needed in app/data/ as input data files for the webapp.

Installation:
cd mslp-webapp
python3 -m virtualenv -p python3 venv
source venv/bin/activate
pip install netCDF4
pip install flask-bootstrap
mkdir app/data
cp mslp_mean.nc mslp_spread.nc app/data/
python3 run.py
